function MarketData() {
  return (
    <section className="panel empty-feature" aria-labelledby="market-heading">
      <p className="section-kicker">Market Data</p>
      <h2 id="market-heading">Market data provider not configured yet</h2>
      <p>
        Live and mock prices are intentionally unavailable until a provider is
        configured.
      </p>
    </section>
  )
}

export default MarketData
